<h1>Discrete Mathematics</h1>


<table>
  <caption>Symbols</caption>
  <tr>
    <td><var>p</var> and <var>q</var></td>
    <td><var>p</var> &and; <var>q</var></td>
  </tr>
  <tr>
    <td><var>p</var> or <var>q</var></td>
    <td><var>p</var> &or; <var>q</var></td>
  </tr>
  <tr>
    <td>not <var>p</var></td>
    <td>&not;<var>p</var></td>
  </tr>
  <tr>
    <td><var>p</var> or <var>q</var> but not both</td>
    <td><var>p</var> &oplus; <var>q</var></td>
  </tr>
  <tr>
    <td><var>P</var> is logically equivalent to <var>Q</var></td>
    <td><var>P</var> &equiv; <var>Q</var></td>
  </tr>
  <tr>
    <td>if <var>p</var> then <var>q</var></td>
    <td><var>p</var> &rarr; <var>q</var></td>
  </tr>
  <tr>
    <td><var>p</var> if and only if <var>q</var></td>
    <td><var>p</var> &harr; <var>q</var></td>
  </tr>
  <tr>
    <td>for all <var>x</var></td>
    <td>&forall;<var>x</var></td>
  </tr>
  <tr>
    <td>there exists <var>m</var></td>
    <td>&exist;<var>m</var></td>
  </tr>
</table>

<h4>Variables</h4>
<dl>
  <dt><var>x</var></dt>
  <dd>Symbol <var>x</var> is placeholder</dd>
  <dt>5<var>x</var> + 2 = <var>x</var><sup>3</sup></dt>
  <dd>Is there a number <var>x</var> that when multiplied
      times 5 and added 2 is equal to its cube?</dd>
</dl>

<h4>Statements</h4>
<dl>
  <dt>Universal statement</dt>
  <dd>Property is true for all elements in set.</dd>
  <dt>Conditional statement</dt>
  <dd>If one property is true another must also be true.</dd>
  <dt>Existential statement</dt>
  <dd>The property is true for at least one thing</dd>
</dl>

<h4>Statement examples</h4>
<dl>
  <dt>For all cats <var>x</var>, if <var>x</var> is black then it belongs to a witch</dt>
  <dd>Universal conditional statement</dd>
  <dt>For all witches <var>x</var>, there is black cat <var>y</var> belonging
      to witch <var>x</var></dt>
  <dd>Universal existential statement</dd>
  <dt>There is a witch who is at least as powerful as all the other witches</dt>
  <dd>Existential universal statement</dd>
</dl>


<h4>Sets</h4>

<ul>
  <li>
    Sets are collections of elements. They can be created using
    <b>set-roster</b> notation. For example, to create a set D containing
    numbers 5, 8 and 12: D = {5, 8, 12}.
  </li>

  <li>
    Another way to create sets is <b>set-builder</b> notation. To get a set of integers
    between 6 and 12 one can write: A = { <var>x</var> &isin; <b>Z</b> | 6 < <var>x</var> < 12 },
    which reads "set of all elements <var>x</var> in <b>Z</b> such that <var>x</var>
    is greater than 6 and less than 12".
  </li>

  <li>
    Some sets in mathematics have their own symbols:
    <ul>
      <li><b>R</b> - real numbers</li>
      <li><b>Z</b> - integers</li>
      <li><b>Q</b> - rational numbers</li>
    </ul>
  </li>

  <li>
    If all elements of a set are elements of another set,
    the first set is called a <b>subset</b>. If A = {3, 6} and
    B = {2, 3, 5, 6, 8}, then A is a subset of B which
    is written as A &sube; B.
  </li>

  <li>
    An <b>ordered pair</b> (a,b) is a pair of objects whose order matters.
    That means that the pair (a,b) is equal to the pair (c,d) only
    if a = c and b = d.
  </li>

  <li>
    <b>Cartesian product</b> of sets A and B is the set of all ordered
    pairs such that the first element of the pair is from set A and
    the second element is from set B. If A = {1, 2} and B = {3, 4}
    then A &times B = {{1, 3}, {1, 4}, {2, 3}, {2, 4}}.
  </li>

  <li>
    <b>Relation</b> R from A to B is a subset of the cartesian product A &times B.
    If (x,y) &isin; R, the notation x R y can be used. Sets A and B are called
    domain and co-domain respectively.
  </li>

</ul>

<h4>Functions</h4>

<ul>

  <li>
    A <b>function</b> F from set A to set B is a relation in which the domain is set A
    and the co-domain is set B. Each element from the first set has a unique
    corresponding element from the second set.
  </li>

  <li>
  </li>

</ul>

